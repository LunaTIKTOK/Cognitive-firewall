[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cognitive-firewall-aperture"
version = "0.0.0"
description = "Runtime governance and uncertainty-pricing infrastructure for AI agent execution."
requires-python = ">=3.10"
